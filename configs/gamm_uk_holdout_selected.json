{
  "selection_id": "uk-gamm-heldout-v1",
  "selection_policy": "Leave-one-UK-radar-out model selection over 14 July 2025 to 13 July 2026. No hard-coded season, solar, or time-of-day exclusion is applied.",
  "gamm_options": {
    "spatial_k": 10,
    "meteorology_interactions": [],
    "temporal_smooths": ["day_of_year", "utc_hour"],
    "temporal_interactions": ["seasonal_diurnal"],
    "target_overrides": {
      "mtr_birds_km_h": {
        "intensity_transform": "sqrt",
        "intensity_weights": "mtr_power",
        "intensity_weight_power": 0.2
      },
      "vid_birds_per_km2": {
        "intensity_transform": "cube_root",
        "intensity_weights": "profile_count"
      },
      "bird_u_ms": {
        "vector_weights": "uniform"
      },
      "bird_v_ms": {
        "vector_weights": "uniform"
      }
    }
  },
  "known_limitation": "LP velocity transfer remains weak under leave-one-radar-out validation. Product metadata must retain this limitation and should not imply calibrated LP flow direction away from reporting radars."
}
